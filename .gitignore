# secrets
.env

# go
api/bin/
api/highfivelowfive

# node
node_modules/
web/dist/
web/.vite/

# uploaded avatars — user data, not source; MEDIA_DIR points here by default
media/

# os
.DS_Store

# seeded design canvas — 2.4MB of bundled editor, rebuilt from the .dc.html sources
design/high-five-low-five-screens.html
